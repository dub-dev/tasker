{% extends "base.html.twig" %}

{% block content %}
    <div class="intro-header">
        <div class="row">
            <div class="col-lg-12">
                <div class="intro-message">
                    <h1>Tasker</h1>
                    <h3>work smart, work fast.</h3>
                    <hr class="intro-divider">
                    <ul class="list-inline intro-social-buttons">
                        <li>
                            <a href="{{ path('fos_user_security_login') }}" class="btn btn-default btn-lg home-btn"><span class="network-name">Login</span></a>
                        </li>
                        <li>
                            <a href="{{ path('fos_user_registration_register') }}" class="btn btn-default btn-lg home-btn"><span class="network-name">Register</span></a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
