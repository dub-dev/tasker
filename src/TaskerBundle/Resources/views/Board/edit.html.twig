{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Board edit</h1>

    {{ form(edit_form) }}



        <a class="btn btn-default pull-right" href="{{ path('board') }}">Back to the list</a>

    {{ form(delete_form) }}

{% endblock %}
